# CabLog environment template
# Copy to .env and fill in values as needed.

# Gemini API key (optional if using LM Studio only)
VITE_GEMINI_API_KEY=""

# LM Studio token (optional, only if your LM Studio server requires auth)
VITE_LM_STUDIO_API_TOKEN=""

# Optional app URL (used by hosted environments)
VITE_APP_URL=""
